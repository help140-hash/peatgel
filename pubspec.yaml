name: peatgel
description: A plant recognition app using Pl@ntNet API
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
  http: ^1.1.0
  image_picker: ^1.0.4

flutter:
  uses-material-design: true
  generate: true
